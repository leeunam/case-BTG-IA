# Durante desenvolvimento, o Vite faz proxy para a API automaticamente via vite.config.ts
# Não é necessário VITE_API_URL em desenvolvimento local.

# Em produção, defina a URL da API se o frontend e o backend estiverem em domínios diferentes:
# VITE_API_URL=https://api.btg-fii.internal
